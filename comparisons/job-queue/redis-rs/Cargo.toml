[package]
name = "job-queue-redis-rs"
version = "0.1.0"
edition = "2024"

[dependencies]
redis = { version = "0.27", features = ["tokio-comp", "streams"] }
tokio = { version = "1", features = ["rt-multi-thread", "macros", "time", "sync"] }
