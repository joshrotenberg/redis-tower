[package]
name = "leaderboard-redis-rs"
version = "0.1.0"
edition = "2024"

[dependencies]
redis = { version = "0.27", features = ["tokio-comp", "aio"] }
tokio = { version = "1", features = ["full"] }
rand = { version = "0.9", features = ["small_rng"] }
